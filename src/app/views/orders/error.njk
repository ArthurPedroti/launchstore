{% extends "layout.njk" %}
{%import "parts/card.njk" as card %}

{% block content %}

  {%call card.render('', 'order-error container')%}

  <div class="error-lottie"></div>
  <h2>Desculpe</h2>
  <p>Não podemos processar o seu pedido</p><br>
  <a href="#" onclick="history.back()">Tente novamente</a>

  {%endcall%}

  <script src="/lottie.min.js"></script>
  <script>
    const element = document.querySelector('.error-lottie')
    lottie.loadAnimation({
      container: element, // the dom element that will contain the animation
      renderer: 'svg',
      loop: false,
      autoplay: true,
      path: '/error.json' // the path to the animation json
    });
  </script>

{% endblock content %}